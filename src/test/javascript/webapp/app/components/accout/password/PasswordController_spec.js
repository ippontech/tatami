/** describe ('Password controller test', function() {
    beforeEach(module('ui.router'));
    beforeEach(module('PasswordModule'));
    beforeEach(inject(function(_$controller_, _$translate_) {
       $controller = _$controller_;
       $translate = _$translate_;
    }));

    it('Password default state', function() {
        var $scope = {};
        var $translate = {};
        var controller = $controller('PasswordController', {$scope: $scope, $translate: $translate});
        console.info($scope);
    });
}); */
